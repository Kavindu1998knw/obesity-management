# Agent Rules

## Browser Subagent Execution Constraint
- **NEVER** launch or execute the `browser_subagent` tool.
- Do not perform browser automation, browser verification, or interactive browser testing.
- Rely on code-level builds, compilation checks, and manual user verification instead.
